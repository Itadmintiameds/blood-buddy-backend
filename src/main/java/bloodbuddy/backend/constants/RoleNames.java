package bloodbuddy.backend.constants;

/** Role names as stored in the roles master table (created manually, never by the app). */
public final class RoleNames {

    public static final String SUPERADMIN = "SUPERADMIN";
    public static final String BLOOD_CENTRE = "BLOOD_CENTRE";

    private RoleNames() {
    }
}
