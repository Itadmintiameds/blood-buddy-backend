spring:
  application:
    name: blood-buddy-backend
  profiles:
    active: dev

server:
  servlet:
    context-path: /api/v1

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui
    enabled: true

